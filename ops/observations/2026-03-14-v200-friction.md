---
date: 2026-03-14T00:00:00Z
type: friction
---

## Harder than it should be
- Edit tool failed on bin/cli.js because the file content differed from the summary (context compaction between sessions). Had to re-read before editing.

## Repeated from last session
- None

## Took longer than expected
- check_absent helper: the test file already had the wrong check() calls written in — had to grep for exact line content to find the mismatch.

## Environment is missing something
- None — Node.js hooks remove the last platform gap.
