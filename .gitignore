# Local Claude Code settings (machine-specific, never commit)
.claude/settings.local.json

# Generated by setup/evolve — local to this machine
.claude/agents/
.claude/capabilities/
.claude/commands/
.claude/scripts/

# Session state — tracked for copilot (memory IS the product state)
# .claude/memory/ — NOT ignored, copilot needs goals.md + checkpoints in git
ops/evolution-log.md
ops/observations/*.md

# Dependencies
node_modules/

# Environment
.env
.env.*

# OS
.DS_Store
Thumbs.db
