---
date: 2026-03-21T03:00
label: v0.4.6-published-all-gaps-closed
files_in_progress: []
---

## What I'm doing right now
Session complete. v0.4.6 published to npm. All gaps identified in the session assessment have been closed. Remaining work requires a real project run (azcomply).

## Why — key decisions made this session
- security-auditor agent (102 rules, 5 categories): one agent not five — all categories share same tools + output format; splitting would add orchestrator overhead with no benefit at STANDARD scale
- /sentinel command: dispatches to security-auditor when installed, falls back to inline scan — graceful degradation
- pre-tool-use.js expanded from 3 to 10 secret patterns (glpat-, xoxb-, xoxp-, npm_, AIza, sk_live_, SG., PRIVATE KEY added)
- /ship pre-ship gate now calls security-auditor before commit; inline secret grep as fallback
- Runner resilience: copilot-state.json survives crashes; stall detection exits after 3 unchanged sessions; stuck milestone hints injected; retry once on non-zero exit
- Regex bug fixed: \w+ → [\w-]+ so "in-progress" status parsed correctly (was captured as "in" — bug confirmed by smoke test)
- Memory pruning: stop.js now prunes checkpoints to 5 most recent on every session end
- CI expanded: 10 required files, agent + command registration check, security-auditor integrity (9 assertions)
- EU AI Act MCP (vinkius-labs/mcp-server-eu-ai-act) evaluated — dropped, user chose not to integrate
- intellgent.md dangling file deleted

## What I know that isn't written down yet
- v0.4.6 is the current published version (not v0.4.4 as goals.md still states — goals.md "Current threads" is stale)
- copilot-state.json will be created at .claude/copilot-state.json on first copilot run — survives runner crash, tracks planHash + stalls + stuckMilestones + retries
- The \w+ → [\w-]+ fix was a real bug: any project using copilot with "in-progress" milestones was never counting them correctly in the progress display
- 1196 tests total (11 node smoke tests pass including 5 new pipeline tests)
- architecture-advisor skill is in both templates/skills/ (ships to users) and .claude/skills/ (local dev install) — this is correct and expected

## What's next (top 3)
1. Update goals.md "Published to npm" line from v0.4.4 to v0.4.6
2. Test on azcomply — npx azclaude@latest install + /sentinel + /copilot run
3. Test --deep copilot mode end-to-end on a real project

## Risk / open question
- goals.md still says "Published to npm: azclaude-copilot@0.4.4" — stale, should be v0.4.6
- /sentinel agent dispatch only checks .claude/agents/ (project-local), not global ~/.claude/agents/. Low risk — most users install per-project
- 102 rules count in security-auditor.md is asserted but not dynamically counted — could drift if rules edited without updating the count string
