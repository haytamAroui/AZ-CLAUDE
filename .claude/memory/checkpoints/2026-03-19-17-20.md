---
date: 2026-03-19T17:20
label: v0.3.5-ci-added
files_in_progress: []
---

## What I'm doing right now
All development complete for this session. v0.3.5 published with GitHub Actions CI. No code changes in flight.

## Why — key decisions made this session
- v0.1.1-0.1.9: 9 fixes from first real copilot test on certification project
- v0.2.0: Skills-first philosophy — minimum 2 project-specific skills mandatory on /setup, evolve threshold lowered from 5 to 2
- v0.2.1-0.2.3: Rebrand AZCLAUDE COPILOT → AZCLAUDE (copilot is one feature, not the product)
- v0.3.0: All 5 review priorities fixed in parallel (3 agents): capability refs, content audit, --deep, conflicts
- v0.3.1: quality-check.md verifies capability refs + fixed stale command names (review→audit, plan→blueprint)
- v0.3.2-0.3.3: --update flag for refreshing stale templates, flag forwarding through subcommand routing
- v0.3.4: Hook captures all tools (Read|Write|Edit|Bash|Grep) + tool sequences for richer reflex patterns
- v0.3.5: GitHub Actions CI — Node 18/20/22 matrix, live badge

## What I know that isn't written down yet
- npm token shared in chat — user needs to revoke at npmjs.com/settings/matyah00/tokens
- Certification project had .opencode/ stale dir from wrong CLI detection (fixed in v0.1.8, warned in v0.3.0)
- The 3 intelligence/ capability ref warnings are expected (only installed with --full flag)
- /reflect first run logged to ops/observations/ but file is gitignored — local only

## What's next (top 3)
1. Test AZCLAUDE on azcomply project (larger production case)
2. v0.4 planning: smarter runner prompt, reflex noise filtering, tool sequence patterns
3. Consider renaming npm package from azclaude-copilot to azclaude (npm deprecate + new package)

## Risk / open question
- CI may fail on Node 18 if test-features.sh uses bash features not available on ubuntu-latest
- npm package name still "azclaude-copilot" — mismatch with rebrand to "AZCLAUDE"
- --deep copilot mode not yet tested end-to-end
