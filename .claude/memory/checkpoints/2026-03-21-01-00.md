---
date: 2026-03-21T01:00
label: v0.4.4-security-hook-published
files_in_progress: []
---

## What I'm doing right now
All work complete. v0.4.4 published to npm with PreToolUse security hook, rewritten README, and three-tier intelligent copilot agents.

## Why — key decisions made this session
- Three-tier copilot (orchestrator + problem-architect + milestone-builder): Orchestrator owns plan.md and never writes code; problem-architect is read-only analyst returning Team Spec; milestone-builder is base builder with pre-read protocol and fix budget
- intelligent-dispatch.md shared capability: Rather than duplicating problem-architect invocation in each command, one shared capability loaded by /add, /fix, /dream, /audit, /refactor, /ship
- PreToolUse hook (pre-tool-use.js): Real-time security interception before Edit/Write/MultiEdit — blocks hardcoded secrets (exit 2), warns on eval/exec/pickle/XSS/GitHub Actions injection. Mirrors Anthropic's official security-guidance plugin in Node.js instead of Python
- GitHub Actions injection added to security.md: `${{ github.event.* }}` in run: steps is HIGH risk — added as first pattern row
- README rewritten product-first: Leads with "CLAUDE.md + memory files = best way to work with an LLM" thesis, covers ALL features (not just v0.4), v0.4 agents are one section not the lead
- CI added new steps: package structure verification (6 required files), CLI AGENTS array check, npm pack --dry-run on Node 22
- Session dedup in pre-tool-use: same file:rule combo warned once per session (temp file in os.tmpdir())

## What I know that isn't written down yet
- pre-tool-use.js is now active in .claude/hooks/ — the dev environment itself has live security interception
- v0.4.4 is the 20th+ npm release; package.json description still says "10 agents" which is correct (7 framework + 3 new copilot team)
- The Anthropic security-guidance plugin uses Python + .claude-plugin/plugin.json format; AZCLAUDE's version uses Node.js and installs via azclaude install — they are compatible (different hook systems, same patterns)
- npm publish showed auto-correction warnings on bin names — these are cosmetic, no impact on functionality
- goals.md "Current threads" still references v0.3.5 as last published — stale, should be updated to v0.4.4

## What's next (top 3)
1. Test on azcomply project (larger production case) — v0.4.4 install + /setup + /copilot run
2. Test --deep copilot mode end-to-end (Opus orchestration path)
3. Update goals.md "Current threads" to reflect v0.4.4 published state

## Risk / open question
- CI .github/workflows/tests.yml doesn't yet verify pre-tool-use.js exists in package structure check — should add it
- npm bin name warnings (auto-corrected) — consider running `npm pkg fix` to clean package.json
- --deep copilot mode not tested end-to-end; orchestrator model selection (Sonnet default, Opus via --deep) not verified in real run
