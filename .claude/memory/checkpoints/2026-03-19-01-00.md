---
date: 2026-03-19T01:00
label: copilot-tested-v0.1.9
files_in_progress: []
---

## What I'm doing right now
All copilot testing complete. 9 releases shipped (v0.1.1→v0.1.9). 1048 tests passing across 37 commits. Copilot tested on certification project — setup generated 3 project-specific skills.

## Why — key decisions made this session
- Copilot auto-installs AZCLAUDE before first session (cli.js runs if .claude/commands/ empty)
- Copilot prompt tells Claude to read .claude/commands/*.md files directly (slash commands don't work in -p mode)
- Added setup/init/install/doctor subcommands to copilot CLI
- /setup Step 5-6: generates project-specific skills and agents from stack/domain analysis
- /evolve Step 7: generates skills from git evidence, agents from co-change clusters — runs AFTER loop-controller (not blocked by STOP HERE)
- evolution/ capabilities now in default install (not gated behind --full)
- Installer always adds missing capability dirs on re-install (not just with --full)
- CLI detection prefers existing project config dir (.claude/) over PATH detection
- generate.md references level3-skills.md + level5-agents.md for quality conventions

## What I know that isn't written down yet
- npm token was shared in chat — user should revoke and regenerate at npmjs.com/settings/matyah00/tokens
- The copilot was tested on a certification website project (React + TS + Vite + Tailwind)
- /setup successfully generated 3 skills: new-page.md, new-component.md, add-exam-questions.md

## What's next (top 3)
1. Test copilot on azcomply project (larger, real production case)
2. v0.2 planning: smarter runner prompt, reflex noise filtering
3. GitHub CodeQL + Secret Scanning on repo settings

## Risk / open question
- Copilot cannot run inside Claude Code Bash tool (2min timeout) — must run in standalone terminal
- Skills auto-invocation doesn't work in -p mode — only works in interactive Claude Code
