---
date: 2026-03-18T03:45
label: agents-created
files_in_progress: []
---

## What I'm doing right now
Created 3 project-specific agents (cc-template-author, cc-cli-integrator, cc-test-maintainer) to support ROADMAP execution. All wired into bin/cli.js AGENTS array. 886 tests passing. Setup complete — ready to start Phase 4 or begin using copilot on a real project.

## Why — key decisions made this session
- Created 3 workstream-specific agents instead of generic ones: each maps to a ROADMAP workstream (templates, CLI, tests)
- All agents follow 5-layer structure from capabilities/shared/5-layer-agent.md — consistent with existing code-reviewer and test-writer agents
- Used `cc-` prefix for project agents — matches AZCLAUDE convention (cc-template-author, cc-cli-integrator, cc-test-maintainer)
- Added agents to AGENTS array in bin/cli.js so they get installed via `installAgents()` when users run `npx azclaude`
- 24 new tests added for the 3 agents — covers existence, 5-layer structure, domain knowledge, self-correction

## What I know that isn't written down yet
- Phases 1-3 were already built in a prior session (copilot.md, plan-tracker.md, copilot.js) — goals.md "Next actions" is stale
- goals.md Next actions still says "Phase 1-3" but those are done — should be updated to Phase 4-5
- AZCLAUDE-copilot-roadmap.md is a duplicate of ROADMAP.md — can be deleted
- The 3 new agents are in templates/agents/ but loop-controller.md is also there (inherited from AZCLAUDE) and not in the AGENTS install array
- Test count jumped from 862 → 886 with the agent tests

## What's next (top 3)
1. Phase 4: Wire /dream → /plan → /add → /evolve → /review → /ship into copilot flow (mostly template wiring)
2. Phase 5: Agent emergence — zero new code, /evolve already handles it
3. Test copilot by running it on azcomply project (the proof case from ROADMAP)

## Risk / open question
- --dangerously-skip-permissions in copilot.js: still no sandboxing/guardrails
- goals.md Next actions section is stale — still shows Phase 1-3 as next
- loop-controller.md exists in templates/agents/ but isn't in AGENTS install array — intentional or oversight?
