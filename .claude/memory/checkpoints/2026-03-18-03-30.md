---
date: 2026-03-18T03:30
label: project-initialized
files_in_progress: []
---

## What I'm doing right now
AZCLAUDE Copilot repo initialized. Forked from AZCLAUDE v1.0.0 with project-scoped hooks, atomic writes, Node smoke tests. ROADMAP.md defines the 5-phase build plan. Ready to start Phase 1.

## Why — key decisions made this session
- New repo (AZ-CLAUDE-COPILOT) instead of building inside AZCLAUDE — separate product, separate concerns
- ROADMAP.md renamed from AZCLAUDE-copilot-roadmap.md — serves as the build spec
- Runner must be Node.js (copilot.js), not bash (copilot.sh) — cross-platform is a core AZCLAUDE value
- Goals.md and checkpoints reset — clean slate, no AZCLAUDE history noise

## What I know that isn't written down yet
- The ROADMAP.md says copilot.sh (bash) but we agreed to use copilot.js (Node.js) instead
- ROADMAP says "817 tests" but the codebase has 830 tests now
- --dangerously-skip-permissions is a security concern flagged in analysis but not addressed in ROADMAP
- Phase 4 (intelligence integration) is mostly wiring, not new code
- Phase 5 (agent emergence) needs zero new code — /evolve already does it

## What's next (top 3)
1. Create templates/commands/copilot.md — the /copilot command
2. Create templates/capabilities/shared/plan-tracker.md — structured milestone format
3. Update templates/commands/plan.md — machine-readable plan.md output

## Risk / open question
- --dangerously-skip-permissions: what sandboxing/guardrails for autonomous mode?
- Token budget: no cost estimation or spending limits in the roadmap
- Deploy step: needs concrete per-platform implementation (Vercel, Railway, etc.)
